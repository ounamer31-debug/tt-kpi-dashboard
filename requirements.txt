# Librairies necessaires pour LANCER LE DASHBOARD (app.py).
# C'est ce fichier que Streamlit Community Cloud utilise au deploiement.
# Le dashboard lit seulement les CSV : il n'a pas besoin de Prophet/scipy.
# (versions volontairement souples pour s'installer sur n'importe quelle
#  version recente de Python en ligne)
streamlit>=1.40
pandas>=2.2
plotly>=5.22
numpy>=1.26
fpdf2>=2.7